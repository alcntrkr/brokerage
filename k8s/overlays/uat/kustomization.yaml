resources:
  - ../../base

configMapGenerator:
  - name: brokerage-config
    files:
      - application.yaml=config/application.yaml

images:
  - name: brokerage
    newTag: uat-latest

patches:
  - path: patch.yaml


generatorOptions:
  disableNameSuffixHash: true